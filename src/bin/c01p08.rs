/*
 * PROBLEM:
 * Rotate Matrix: Given an image represented by an NxN matrix, where each pixel in the image is 4
 * bytes, write a method to rotate the image by 90 degrees. Can you do this in place?
*/

#[derive(Debug)]
struct Image;

fn rotate_matrix(img: Image) -> Image {
    Image {}
}

fn rotate_matrix_in_place(img: Image) -> Image {
    Image {}
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_rotate_matrix() {
        let img = Image {};
        let rotated_img = rotate_matrix(img);
        assert_eq!(rotated_img, img);
    }
}

fn main() {}
